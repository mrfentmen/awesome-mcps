import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import { z } from "zod"
import { format, info, KevError, recent, search } from "./api.js"
const text = (s: string) => ({ content: [{ type: "text" as const, text: s }] })
export function createServer() { const server = new McpServer({ name: "cisa-kev-mcp", version: "1.0.0" }); server.tool("search_known_exploited", "Search CISA's catalog of vulnerabilities known to be exploited in the wild.", { query: z.string().min(1) }, async ({ query }) => { try { return text(format(await search(query))) } catch (e) { return text(`Error: ${e instanceof Error ? e.message : String(e)}`) } }); server.tool("recent_known_exploited", "List the most recently added entries in the CISA KEV catalog.", { limit: z.number().int().min(1).max(50).default(10) }, async ({ limit }) => { try { return text(format(await recent(limit))) } catch (e) { return text(`Error: ${e instanceof Error ? e.message : String(e)}`) } }); server.tool("catalog_info", "Get CISA KEV catalog version and release metadata.", {}, async () => { try { return text(format(await info())) } catch (e) { return text(`Error: ${e instanceof Error ? e.message : String(e)}`) } }); return server }
export { KevError }
