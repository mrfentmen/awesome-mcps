import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import { z } from "zod"
import { entity, format, search, sparql, WikidataError } from "./api.js"
const text = (s: string) => ({ content: [{ type: "text" as const, text: s }] })
export function createServer() { const server = new McpServer({ name: "wikidata-mcp", version: "1.0.0" }); server.tool("search_entities", "Search Wikidata entities by name or description.", { query: z.string().min(1) }, async ({ query }) => { try { return text(format(await search(query))) } catch (e) { return text(`Error: ${e instanceof Error ? e.message : String(e)}`) } }); server.tool("get_entity", "Get public Wikidata labels, descriptions, claims, and sitelinks by Q ID.", { id: z.string().regex(/^Q\d+$/i) }, async ({ id }) => { try { return text(format(await entity(id.toUpperCase()))) } catch (e) { return text(`Error: ${e instanceof Error ? e.message : String(e)}`) } }); server.tool("query_knowledge", "Run a bounded read-only SPARQL query against Wikidata. Keep queries selective and include LIMIT.", { query: z.string().min(1).max(8000) }, async ({ query }) => { try { return text(format(await sparql(query))) } catch (e) { return text(`Error: ${e instanceof Error ? e.message : String(e)}`) } }); return server }
export { WikidataError }
