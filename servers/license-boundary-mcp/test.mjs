import assert from "node:assert/strict";import {mkdtemp,mkdir,writeFile,rm} from "node:fs/promises";import path from "node:path";import os from "node:os";import {inspectLicenseBoundary} from "./dist/core.js";const root=await mkdtemp(path.join(os.tmpdir(),"license-boundary-"));try{const p=path.join(root,"p");await mkdir(p);await writeFile(path.join(p,"LICENSE"),"MIT License PRIVATE");process.env.LICENSE_BOUNDARY_ROOT=root;const r=await inspectLicenseBoundary({project:"p"});assert.equal(r.licenseEvidenceCounts.mit,1);assert.equal(JSON.stringify(r).includes("PRIVATE"),false)}finally{await rm(root,{recursive:true,force:true})}console.log("LICENSE BOUNDARY TEST PASSED")
