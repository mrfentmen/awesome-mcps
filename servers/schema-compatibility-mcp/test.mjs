import assert from "node:assert/strict";import {mkdtemp,writeFile,rm} from "node:fs/promises";import path from "node:path";import os from "node:os";import {compareSchemaShapes} from "./dist/core.js";const root=await mkdtemp(path.join(os.tmpdir(),"schema-compat-"));try{await writeFile(path.join(root,"before.json"),JSON.stringify({private:"SECRET",items:[1]}));await writeFile(path.join(root,"after.json"),JSON.stringify({private:"SECRET",items:[1,"x"]}));process.env.SCHEMA_COMPATIBILITY_ROOT=root;const r=await compareSchemaShapes({before:"before.json",after:"after.json"});assert.ok(r.changedTypeCount>=1);assert.equal(JSON.stringify(r).includes("SECRET"),false)}finally{await rm(root,{recursive:true,force:true})}console.log("SCHEMA COMPATIBILITY TEST PASSED")
