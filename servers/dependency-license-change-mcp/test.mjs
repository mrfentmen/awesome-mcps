import assert from "node:assert/strict";import {mkdtemp,mkdir,writeFile,rm} from "node:fs/promises";import path from "node:path";import os from "node:os";import {compareLicenseEvidence} from "./dist/core.js";const root=await mkdtemp(path.join(os.tmpdir(),"license-change-"));try{for(const [n,c] of [["before","MIT License PRIVATE"],["after","Apache License 2 PRIVATE"]]){const p=path.join(root,n);await mkdir(p);await writeFile(path.join(p,"LICENSE"),c)}process.env.LICENSE_CHANGE_ROOT=root;const r=await compareLicenseEvidence({before:"before",after:"after"});assert.equal(r.changedCategoryCount,2);assert.equal(JSON.stringify(r).includes("PRIVATE"),false)}finally{await rm(root,{recursive:true,force:true})}console.log("LICENSE CHANGE TEST PASSED")
