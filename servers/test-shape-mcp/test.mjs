import assert from "node:assert/strict";import {mkdtemp,mkdir,writeFile,rm} from "node:fs/promises";import path from "node:path";import os from "node:os";import {inspectTestShape} from "./dist/core.js";const root=await mkdtemp(path.join(os.tmpdir(),"test-shape-"));try{const p=path.join(root,"p");await mkdir(p);await writeFile(path.join(p,"private.test.js"),"test('PRIVATE',()=>{ expect(1).toBe(1) })");process.env.TEST_SHAPE_ROOT=root;const r=await inspectTestShape({project:"p"});assert.equal(r.testBlocks,1);assert.equal(r.assertionFiles,1);assert.equal(JSON.stringify(r).includes("PRIVATE"),false)}finally{await rm(root,{recursive:true,force:true})}console.log("TEST SHAPE TEST PASSED")
