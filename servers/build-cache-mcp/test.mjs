import assert from "node:assert/strict";import {mkdtemp,mkdir,writeFile,rm} from "node:fs/promises";import path from "node:path";import os from "node:os";import {inspectBuildCache} from "./dist/core.js";const root=await mkdtemp(path.join(os.tmpdir(),"build-cache-"));try{const p=path.join(root,"p","packages","app","dist");await mkdir(p,{recursive:true});await writeFile(path.join(p,"PRIVATE.js"),"PRIVATE");process.env.BUILD_CACHE_ROOT=root;const r=await inspectBuildCache({project:"p"});assert.equal(r.cacheOrArtifactDirectories,1);assert.equal(r.filesObserved,1);assert.equal(r.valueFree,true);assert.equal(JSON.stringify(r).includes("PRIVATE"),false)}finally{await rm(root,{recursive:true,force:true})}console.log("BUILD CACHE TEST PASSED")
