{
  "name": "open-meteo-mcp",
  "version": "1.0.0",
  "description": "Use this MCP server to weather forecasts from Open Meteo. No API key, 7 day forecasts, geocoding. Tools include geocode, get forecast, get forecast for place",
  "type": "module",
  "mcpName": "io.github.mrfentmen/open-meteo-mcp",
  "repository": {
    "type": "git",
    "url": "https://github.com/mrfentmen/open-meteo-mcp.git"
  },
  "bin": {
    "open-meteo-mcp": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "rm -rf dist && tsc -p tsconfig.json && node dist/index.js",
    "inspect": "npx @modelcontextprotocol/inspector node dist/index.js"
  },
  "keywords": [
    "mcp",
    "open-meteo",
    "api"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
