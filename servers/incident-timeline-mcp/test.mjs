import assert from "node:assert/strict";import {mkdtemp,mkdir,writeFile,rm} from "node:fs/promises";import path from "node:path";import os from "node:os";import {summarizeIncidentTimeline} from "./dist/core.js";const root=await mkdtemp(path.join(os.tmpdir(),"incident-timeline-"));try{const p=path.join(root,"p");await mkdir(p);await writeFile(path.join(p,"private.log"),"2026-01-01 00:00:00 ERROR PRIVATE\n");process.env.INCIDENT_TIMELINE_ROOT=root;const r=await summarizeIncidentTimeline({project:"p"});assert.equal(r.logFiles,1);assert.equal(r.levelCounts.error,1);assert.equal(JSON.stringify(r).includes("PRIVATE"),false)}finally{await rm(root,{recursive:true,force:true})}console.log("INCIDENT TIMELINE TEST PASSED")
