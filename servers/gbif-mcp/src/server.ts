import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import { z } from "zod"
import { format, match, occurrences, species, GbifError } from "./api.js"
const text = (s: string) => ({ content: [{ type: "text" as const, text: s }] })
export function createServer() { const server = new McpServer({ name: "gbif-mcp", version: "1.0.0" }); server.tool("match_species", "Match a scientific or common name to GBIF taxonomy.", { name: z.string().min(1) }, async ({ name }) => { try { return text(format(await match(name))) } catch (e) { return text(`Error: ${e instanceof Error ? e.message : String(e)}`) } }); server.tool("get_species", "Get GBIF taxonomy metadata by usage key.", { key: z.string().min(1) }, async ({ key }) => { try { return text(format(await species(key))) } catch (e) { return text(`Error: ${e instanceof Error ? e.message : String(e)}`) } }); server.tool("search_occurrences", "Search public biodiversity observations by taxon, country, year, or bounding box.", { taxonKey: z.string().optional(), country: z.string().length(2).optional(), year: z.string().optional(), geometry: z.string().optional(), limit: z.number().int().min(1).max(100).default(20) }, async ({ taxonKey, country, year, geometry, limit }) => { try { return text(format(await occurrences({ ...(taxonKey ? { taxonKey } : {}), ...(country ? { country } : {}), ...(year ? { year } : {}), ...(geometry ? { geometry } : {}), limit: String(limit) }))) } catch (e) { return text(`Error: ${e instanceof Error ? e.message : String(e)}`) } }); return server }
export { GbifError }
