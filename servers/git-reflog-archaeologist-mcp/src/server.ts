import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import { z } from "zod"
import { format, recoverySignals, reflogSignals, stashSignals } from "./archaeology.js"
const text = (value: string) => ({ content: [{ type: "text" as const, text: value }] })
const errorText = (error: unknown) => text(`Error: ${error instanceof Error ? error.message : "Local Git archaeology failed"}`)
export function createServer() { const server = new McpServer({ name: "git-reflog-archaeologist-mcp", version: "1.0.0" }); const cwd = z.string().min(1).max(1000).default(".").describe("Local Git repository path; no network access is used."); server.tool("reflog_signals", "Summarize local reflog activity by count and age bucket without returning ref names, hashes, subjects, paths, or remotes.", { cwd }, async ({ cwd: directory }) => { try { return text(format(await reflogSignals(directory))) } catch (error) { return errorText(error) } }); server.tool("recovery_signals", "Count unreachable Git object types and recovery hints without returning object IDs, messages, paths, or contents.", { cwd }, async ({ cwd: directory }) => { try { return text(format(await recoverySignals(directory))) } catch (error) { return errorText(error) } }); server.tool("stash_signals", "Summarize local stash age buckets and count without returning stash messages, refs, hashes, or paths.", { cwd }, async ({ cwd: directory }) => { try { return text(format(await stashSignals(directory))) } catch (error) { return errorText(error) } }); return server }
