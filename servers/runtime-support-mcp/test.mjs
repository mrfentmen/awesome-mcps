import assert from "node:assert/strict";import {mkdtemp,mkdir,writeFile,rm} from "node:fs/promises";import path from "node:path";import os from "node:os";import {inspectRuntimeSupport} from "./dist/core.js";const root=await mkdtemp(path.join(os.tmpdir(),"runtime-support-"));try{const p=path.join(root,"p");await mkdir(p);await writeFile(path.join(p,"package.json"),"{\"engines\":{\"node\":\">=20 PRIVATE\"}}");process.env.RUNTIME_SUPPORT_ROOT=root;const r=await inspectRuntimeSupport({project:"p"});assert.equal(r.filesScanned,1);assert.ok(r.runtimeFamilySignals.node);assert.equal(JSON.stringify(r).includes("PRIVATE"),false)}finally{await rm(root,{recursive:true,force:true})}console.log("RUNTIME SUPPORT TEST PASSED")
