import assert from "node:assert/strict";import {mkdtemp,mkdir,writeFile,rm} from "node:fs/promises";import path from "node:path";import os from "node:os";import {inspectDeprecationRadar} from "./dist/core.js";const root=await mkdtemp(path.join(os.tmpdir(),"deprecation-radar-"));try{const p=path.join(root,"p");await mkdir(p);await writeFile(path.join(p,"private.ts"),"/** deprecated PRIVATE endpoint */");process.env.API_DEPRECATION_ROOT=root;const r=await inspectDeprecationRadar({project:"p"});assert.equal(r.markerCount,1);assert.equal(r.valueFree,true);assert.equal(JSON.stringify(r).includes("PRIVATE"),false)}finally{await rm(root,{recursive:true,force:true})}console.log("DEPRECATION RADAR TEST PASSED")
