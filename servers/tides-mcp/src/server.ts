import { McpServer } from "@modelcontextprotocol/sdk/server/mcp.js"
import { z } from "zod"
import { format, predictions, TidesError, waterLevels } from "./api.js"
const text = (s: string) => ({ content: [{ type: "text" as const, text: s }] })
export function createServer() { const server = new McpServer({ name: "tides-mcp", version: "1.0.0" }); server.tool("get_tide_predictions", "Get NOAA high low or hourly tide predictions for a station and date range.", { station: z.string().min(3), begin: z.string().describe("YYYY-MM-DD"), end: z.string().describe("YYYY-MM-DD"), interval: z.enum(["hilo", "h"]).default("hilo"), units: z.enum(["english", "metric"]).default("english") }, async ({ station, begin, end, interval, units }) => { try { return text(format(await predictions(station, begin, end, interval, units))) } catch (e) { return text(`Error: ${e instanceof Error ? e.message : String(e)}`) } }); server.tool("get_water_levels", "Get observed NOAA water levels for a station and short date range.", { station: z.string().min(3), begin: z.string(), end: z.string(), interval: z.enum(["6", "h"]).default("6"), units: z.enum(["english", "metric"]).default("english") }, async ({ station, begin, end, interval, units }) => { try { return text(format(await waterLevels(station, begin, end, interval, units))) } catch (e) { return text(`Error: ${e instanceof Error ? e.message : String(e)}`) } }); return server }
export { TidesError }
