{
  "name": "vndb-mcp",
  "version": "1.0.0",
  "description": "Use this MCP server to VNDB, the Visual Novel Database. Search VNs, characters, releases, tags, ratings. Tools include search vns, get vn, search characters",
  "type": "module",
  "mcpName": "io.github.mrfentmen/vndb-mcp",
  "repository": {
    "type": "git",
    "url": "https://github.com/mrfentmen/vndb-mcp.git"
  },
  "bin": {
    "vndb-mcp": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "rm -rf dist && tsc -p tsconfig.json && node dist/index.js",
    "inspect": "npx @modelcontextprotocol/inspector node dist/index.js"
  },
  "keywords": [
    "mcp",
    "vndb",
    "visual-novel",
    "anime",
    "galgame",
    "database"
  ],
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
